# Optional: override where notes are persisted on disk (JSON format).
# Default (if unset): notes_backend/data/notes.json
NOTES_PERSIST_PATH=

# Comma-separated list of allowed CORS origins.
# Default (if unset): http://localhost:3000
CORS_ORIGINS=http://localhost:3000
